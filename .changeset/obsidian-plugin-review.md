---
"neuralnotes": patch
---

Resolve Obsidian plugin review feedback: replace `any` types with SDK types, drop the plugin name from command id/name, fix sentence case in UI text, await `revealLeaf` promises, swap inline `style.display` for a CSS class, and tighten async generator/regex/event-handler typing.
